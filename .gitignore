# Ignore any compiled files
*.pyc
# The settings file has secret data in it, so don't track that
settings.py
build/*
# Don't track anything in the files or static folders
static/*
files/*
library/openlibrary/*
library/python-openlibrary/*
# Don't track swp files created by vim
*.swp
